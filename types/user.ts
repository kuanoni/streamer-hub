export enum AuthPerms {
	ADMIN,
	MOD,
	USER,
}

export enum SubscriptionTier {
	NONE,
	TIER_1,
	TIER_2,
	TIER_3,
	PERMANENT,
}

export enum Role {
	BUDDY = 'BUDDY',
	OWNER = 'OWNER',
}

export enum InfoBadge {
	LAWYER = 'LAWYER',
}
